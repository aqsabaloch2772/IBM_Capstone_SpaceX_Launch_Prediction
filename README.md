# IBM Capstone: SpaceX Launch Success Prediction

End-to-end data science project for the IBM Data Science Capstone.
Includes: data collection (API & web scraping), data wrangling, SQL EDA,
visual EDA, Folium map, Plotly Dash dashboard, machine learning models,
and final presentation.

## Repository structure

notebooks/        -> Jupyter notebooks (01 → 07)
data/             -> Raw & processed datasets (CSV)
scripts/          -> Reusable Python scripts (data cleaning, modeling)
results/          -> Figures, model outputs, final_predictions.csv
presentation/     -> Final PPT and PDF

## How to run (local)

1. Clone the repo (or download ZIP from GitHub).
2. Create a virtual environment:
   - macOS / Linux: `python3 -m venv venv && source venv/bin/activate`
   - Windows (PowerShell): `python -m venv venv; .\\venv\\Scripts\\Activate.ps1`
3. Install dependencies: `pip install -r requirements.txt`
4. Run notebooks in order (01 → 07)

## Author
AQSA ABDULLAH 
